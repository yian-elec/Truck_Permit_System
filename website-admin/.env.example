# 後端 API 根位址（勿含尾隨斜線）。不同網域/HTTPS/固定 API 網域時**請設**（建置前寫入 .env.production）。
# 未設時，瀏覽器內會用「目前開頁的 hostname」+ 下面埠，方便用 http://<IP> 從外網/區網連同機後端。
# VITE_API_BASE_URL=https://api.example.com
# 未設 VITE_API_BASE_URL 時，同 host 的 API 埠（預設 8000）
# VITE_API_PORT=8000
VITE_APP_NAME=Truck Permit Admin
